# Optional: Only needed if you want to automate the initial SSO login
# After first manual login, the browser state is saved and these aren't required
ASIMUT_EMAIL=your.email@student.rwcmd.ac.uk

# Note: For Microsoft 365 SSO, you'll typically do the first login manually
# in a headed browser, which saves the session for future headless runs
