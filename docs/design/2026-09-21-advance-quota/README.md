# Advance quota editor

The user selected **A — Compact settings** on 21 September 2026. All three
editable SVG boards include desktop/mobile layouts and the relevant editor,
loading, validation, save, cancellation, conflict and help states. Example
allocations are invented. B and C are alternatives, not implementation targets.

A adds a dedicated Settings entry and a short editor with expandable room/period,
weekday and waiting/reserve controls. Desktop and phone share the same settings
schema and preserve the existing application style and other settings. Detailed
semantics and verification are in ../../advance-quota-preferences.md.
